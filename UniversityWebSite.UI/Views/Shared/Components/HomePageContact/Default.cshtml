@model Contact

<div class="contentIn">
    <div>
        <h1>Anasayfa - İletişim Bölümü</h1>

        <div class="form-group">
            <label>Oluşturulma Tarihi</label>
            <input type="text" class="form-control" value="@Model.CreatedTime" readonly>
        </div>

        <div class="form-group">
            <label>Güncellenme Tarihi</label>
            <input type="text" class="form-control" value="@Model.UpdatedTime" readonly>
        </div>

        <div class="form-group">
            <label>E-mail</label>
            <input type="text" class="form-control" value="@Model.Email" required>
        </div>

        <div class="form-group">
            <label>Telefon Numarası</label>
            <input type="text" class="form-control" value="@Model.Phone" required>
        </div>

        <div class="form-group">
            <label>Faks Numarası</label>
            <input type="text" class="form-control" value="@Model.Fax" required>
        </div>


        <div class="row justify-content-center">
            <button type="submit" onclick="sendForm()" class="btn btn-primary align-baseline">Güncelle</button>
        </div>

        <div id="error"></div>
    </div>
</div>

<script>
    // Variable Definition

    window.onload = function () {
        // Default Value assign
    }

    function changed(id, changedValue) {

    }

    function sendForm() {
        document.getElementById("error").innerHTML = "";

        if (false) {
            alert("Herhangi bir değişiklik yapmadınız!");
            return;
        }
        else {
            if (false) {
                document.getElementById("error").innerHTML = "Anahtar kelime adı boş veya 3 karakterden küçük olamaz.";
                return;
            }
        }

        $.ajax({
            url: "/Dashboard/UpdateAbout",
            type: "POST",
            data: {
                Id: keywordId, Title: keywordTitle, CreatedTime: "@ViewData["CreatedTime"]"
            },
            success: function () {
                window.location = "/Dashboard/ReadCategory?page=" + "@ViewData["pageName"]";
            }
        });
    }
</script>
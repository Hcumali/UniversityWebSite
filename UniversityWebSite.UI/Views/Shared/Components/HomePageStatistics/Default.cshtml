@model Statistic

<div class="contentIn">
    <div>
        <h1>Anasayfa - İstatistikler Bölümü</h1>

        <div class="form-group">
            <label>Oluşturulma Tarihi</label>
            <input type="text" class="form-control" value="@Model.CreatedTime" readonly>
        </div>

        <div class="form-group">
            <label>Güncellenme Tarihi</label>
            <input type="text" class="form-control" value="@Model.UpdatedTime" readonly>
        </div>

        <div class="form-group">
            <label>Öğrenci Sayısı</label>
            <input type="text" class="form-control" value="@Model.NumberOfStudent">
        </div>

        <div class="form-group">
            <label>Akademisyen Sayısı</label>
            <input type="text" class="form-control" value="@Model.NumberOfTeacher">
        </div>

        <div class="form-group">
            <label>Fakülte Sayısı</label>
            <input type="text" class="form-control" value="@Model.NumberOfFaculty">
        </div>

        <div class="form-group">
            <label>Proje Sayısı</label>
            <input type="text" class="form-control" value="@Model.NumberOfProject">
        </div>


        <div class="row justify-content-center">
            <button type="submit" onclick="sendForm()" class="btn btn-primary align-baseline">Güncelle</button>
        </div>

        <div id="error"></div>
    </div>
</div>

<script>
    function sendForm() {
        //validasyon - sadece sayı girilebilsin. ve hane sınırı getirilsin.
        console.log("working!");
    }
</script>
    <!-- ======= Header ======= -->
<header id="header" class="fixed-top">
    <div class="container d-flex align-items-center">

        <h1 class="logo me-auto"><a href="#">Düzce Üniversitesi</a></h1>

        <nav id="navbar" class="navbar order-last order-lg-0">
            <ul>
                <li class="dropdown">
                    <a href="#"><span>Üniversitemiz</span> <i class="bi bi-chevron-down"></i></a>
                    <ul>
                        @{
                            for (int i = 0; i < Model.Count; i++)
                            {
                                if (Model[i].NavBarHeader == NavBarHeader.University)
                                {
                                    if (Model[i].Subtitles.Count != 0)
                                    {
                                        <li class="dropdown">
                                            <a href="#"><span>@Model[i].Name</span> <i class="bi bi-chevron-right"></i></a>
                                            <ul>
                                                @for (int j = 0; j < Model[i].Subtitles.Count; j++)
                                                {
                                                    <li><a href="#">@Model[i].Subtitles[j].Name</a></li>
                                                }
                                            </ul>
                                        </li>
                                    }
                                    else
                                    {
                                        <li><a href="#">@Model[i].Name</a></li>
                                    }
                                }
                            }
                        }
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#"><span>Akademik</span> <i class="bi bi-chevron-down"></i></a>
                    <ul>
                        @{
                            for (int i = 0; i < Model.Count; i++)
                            {
                                if (Model[i].NavBarHeader == NavBarHeader.Academic)
                                {
                                    if (Model[i].Subtitles.Count != 0)
                                    {
                                        <li class="dropdown">
                                            <a href="#"><span>@Model[i].Name</span> <i class="bi bi-chevron-right"></i></a>
                                            <ul>
                                                @for (int j = 0; j < Model[i].Subtitles.Count; j++)
                                                {
                                                    <li><a href="#">@Model[i].Subtitles[j].Name</a></li>
                                                }
                                            </ul>
                                        </li>
                                    }
                                    else
                                    {
                                        <li><a href="#">@Model[i].Name</a></li>
                                    }
                                }
                            }
                        }
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#"><span>Araştırma</span> <i class="bi bi-chevron-down"></i></a>
                    <ul>
                        @{
                            for (int i = 0; i < Model.Count; i++)
                            {
                                if (Model[i].NavBarHeader == NavBarHeader.Research)
                                {
                                    if (Model[i].Subtitles.Count != 0)
                                    {
                                        <li class="dropdown">
                                            <a href="#"><span>@Model[i].Name</span> <i class="bi bi-chevron-right"></i></a>
                                            <ul>
                                                @for (int j = 0; j < Model[i].Subtitles.Count; j++)
                                                {
                                                    <li><a href="#">@Model[i].Subtitles[j].Name</a></li>
                                                }
                                            </ul>
                                        </li>
                                    }
                                    else
                                    {
                                        <li><a href="#">@Model[i].Name</a></li>
                                    }
                                }
                            }
                        }
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#"><span>İhtisaslaşma</span> <i class="bi bi-chevron-down"></i></a>
                    <ul>
                        @{
                            for (int i = 0; i < Model.Count; i++)
                            {
                                if (Model[i].NavBarHeader == NavBarHeader.Specialization)
                                {
                                    if (Model[i].Subtitles.Count != 0)
                                    {
                                        <li class="dropdown">
                                            <a href="#"><span>@Model[i].Name</span> <i class="bi bi-chevron-right"></i></a>
                                            <ul>
                                                @for (int j = 0; j < Model[i].Subtitles.Count; j++)
                                                {
                                                    <li><a href="#">@Model[i].Subtitles[j].Name</a></li>
                                                }
                                            </ul>
                                        </li>
                                    }
                                    else
                                    {
                                        <li><a href="#">@Model[i].Name</a></li>
                                    }
                                }
                            }
                        }
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#"><span>Hızlı Erişim</span> <i class="bi bi-chevron-down"></i></a>
                    <ul>
                        @{
                            for (int i = 0; i < Model.Count; i++)
                            {
                                if (Model[i].NavBarHeader == NavBarHeader.FastAccess)
                                {
                                    if (Model[i].Subtitles.Count != 0)
                                    {
                                        <li class="dropdown">
                                            <a href="#"><span>@Model[i].Name</span> <i class="bi bi-chevron-right"></i></a>
                                            <ul>
                                                @for (int j = 0; j < Model[i].Subtitles.Count; j++)
                                                {
                                                    <li><a href="#">@Model[i].Subtitles[j].Name</a></li>
                                                }
                                            </ul>
                                        </li>
                                    }
                                    else
                                    {
                                        <li><a href="#">@Model[i].Name</a></li>
                                    }
                                }
                            }
                        }
                    </ul>
                </li>

                <li><a href="#footer">İletişim</a></li>
            </ul>
            <i class="bi bi-list mobile-nav-toggle"></i>
        </nav><!-- .navbar -->

        @{
            if (User.Identity.IsAuthenticated)
            {
                <a class="get-started-btn" asp-controller="Dashboard" asp-action="Index">Yönetim Paneli</a>
            }
            else
            {
                <a class="get-started-btn" asp-controller="Dashboard" asp-action="Index">Giriş Yap</a>
            }
        }

    </div>
</header>